class Solution {
public:
    vector<int> smallerNumbersThanCurrent(vector<int>& nums) {

       /* vector<int> ans;

        int count;

        for(int i=0;i<nums.size();i++){
            
            count=0;
                                                       brute force:O(N*N)
            for(int j=0;j<nums.size();j++){
                if(nums[j]<nums[i]){
                    count++;
                }
            }

            ans.push_back(count);
        }

        return ans;*/


        vector<int> ans;              //concept of hashing 
        
    }
};